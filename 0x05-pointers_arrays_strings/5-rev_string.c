#include "main.h"
/**
 * rev_string - A function that reverses a string
 * @s: string to be reversed
*/
void rev_string(char *s)
{
char *temp;
int i, j;
for (i = 0; s[i] != '\0'; i++)
temp[i] = s[i];
temp[i] = '\0';
i = j;
for ( ; i >= 0; i--)
s[j - i] = temp[i];
}
