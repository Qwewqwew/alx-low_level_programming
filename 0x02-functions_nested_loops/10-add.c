#include <stdio.h>
#include "main.h"
/**
 * add - receives 2 int and add them
 * then returns the result
 *
 * @i: input to be added
 *
 * @j: input to be added
 *
 * Return: return the result
*/
int add(int i, int j)
{int result;
result = i + j;
return (result);
}
